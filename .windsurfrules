# Gemini Browser Agent Skills — IDE Rules

> This file is auto-loaded by Windsurf IDE into every conversation's system prompt.
> It acts as a lightweight router. NEVER preload all skills — load on demand.

## 🧭 SKILLS ROUTER (Dynamic Context)

You have access to specialized skills and rules in this project. To save tokens:
- **DO NOT** preload all skill/rule files
- **DO** check this routing table before every task
- **DO** use `read_file` to load the relevant skill ONLY when needed

### Routing Table

| When | Load This File |
|---|---|
| Before launching Browser Subagent | `skills/browser-preflight.md` |
| After Browser Subagent returns | `skills/browser-cleanup.md` |
| Disk > 10GB or user says "clean" | `skills/browser-heavy-cleanup.md` |
| Any browser task (planning/executing) | `rules/rule-using-browser.md` |

### Always-On Rules (Memorize These — No File Load)

- NEVER run Browser Subagent > 10 steps without pausing
- ALWAYS summarize browser results BEFORE deleting artifacts
- ALWAYS delete `.webp` recordings after browser sessions
- NEVER delete entire `~/.gemini/antigravity` directory

### Self-Check Protocol

Before writing code for any task, briefly list the top 3 rules that apply.
Format: `📋 Active: [rule1], [rule2], [rule3]`

### Error Recovery

If terminal output contains `[AI_SYSTEM_HINT]`, follow those instructions immediately.
If Browser Subagent fails, read `skills/browser-cleanup.md` BEFORE retrying.
